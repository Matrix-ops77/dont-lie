"""dontlie demo scripts — shipped inside the wheel so `dontlie demo` works
after `pip install dontlie`. The scripts are invoked via `python -m` from
`dontlie demo` (cli.cmd_demo) using the package's __file__ as the cwd.
"""
